/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['aceternity.com', 'images.unsplash.com', 'static.wixstatic.com'],

  },
};

export default nextConfig;